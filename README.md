# UNERA Clinic Frontend

Arabic RTL frontend for **عيادة يونيرا**, built with React, Vite, and TypeScript.

## Run locally

```powershell
npm install
npm run dev
```

## Quality checks

```powershell
npm run lint
npm run build
```

The browser communicates through `/api/*` only.
